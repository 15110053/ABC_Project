angular.module("EcommerceModule").controller("NavController", function ($scope, $location, $routeParams){
	/*$scope.navItem = [{
		display: "Home",
		url: "/abcClientApp"
	}];
	this.addNavItem = function(){
		var param = $routeParams;
		console.log(param);
		console.log("id: "+param[0]);
		
		var item = {
			display: $routeParams.categoryName,
			url: "/abcClientApp/product/"+$routeParams.categoryName+"/"+$routeParams.categoryId
		}
		$scope.navItem.push(item);
		console.log($scope.navItem);
	}
	this.addNavItem();*/
});
